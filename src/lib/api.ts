// This file is now deprecated as we're using Convex directly
// All API calls are now handled through Convex queries and mutations
// See convex/ directory for the new backend functions

// Example usage:
// import { useQuery, useMutation } from 'convex/react';
// import { api } from '../../convex/_generated/api';
// 
// const courses = useQuery(api.courses.getAll);
// const createCourse = useMutation(api.courses.create);